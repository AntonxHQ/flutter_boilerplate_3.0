name: antonx_flutter_boilerplate_3
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  device_info_plus: ^9.0.2
  dio: ^5.3.0
  file_picker: ^5.3.3
  firebase_messaging: ^14.6.5
  flutter:
    sdk: flutter
  geocoding: ^2.1.0
  geolocator: ^9.0.2
  get: ^4.6.5
  google_maps_flutter: ^2.3.1
  intl: ^0.18.1
  logger: ^1.4.0
  shared_preferences: ^2.2.0
  stacked: ^3.4.0
  stacked_services: ^1.1.0

dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3

flutter:
  uses-material-design: true

